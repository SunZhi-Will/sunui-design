{
    "buildCommand": "npm install && lerna bootstrap && npm run build:packages && npm run build-storybook",
    "outputDirectory": "storybook-static",
    "installCommand": "npm install --legacy-peer-deps --workspaces"
}